
*** The json database in the db.json file

*** To run the the json-server ( cmd : npm run json-server )
	custom scripts are added in the package.json file.
	
*** To run the application ( cmd : npm start )

*** Externals tools that are used in this project and added to the package.json file :
					1. "@reach/router": "^1.3.3"
					2. "axios": "^0.19.2"
					3. "json-server": "^0.16.1"

